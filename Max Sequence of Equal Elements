package com.company;

import java.util.Arrays;
import java.util.Scanner;

public class Main {

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        int[] array = Arrays.stream(scanner.nextLine().split(" ")).mapToInt(e -> Integer.parseInt(e)).toArray();

        int longestSequenceLength = 0;
        int endIndex = 0;
        int currentSequenceLength = 0;

        for (int i = 0; i < array.length - 1; i++) {

            if (array[i] == array[i + 1]) {
                currentSequenceLength++;
                if (currentSequenceLength > longestSequenceLength) {
                    endIndex = i + 1; /// The end index is i + 1 in order to save the index of the element to which the element at 'i' is equal
                    longestSequenceLength = currentSequenceLength;
                }
            } else {
                currentSequenceLength = 0; /// Make the sequence 0
            }
        }
        while ((longestSequenceLength--) >= 0) {
            System.out.print(array[endIndex--] + " ");
        }
    }
}
